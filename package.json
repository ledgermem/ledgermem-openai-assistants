{
  "name": "@ledgermem/openai-assistants",
  "version": "0.1.0",
  "description": "OpenAI Assistants API compatibility shim backed by LedgerMem.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "keywords": [
    "ledgermem",
    "openai",
    "assistants",
    "shim",
    "compatibility",
    "memory"
  ],
  "author": "LedgerMem <founders@proofly.dev>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ledgermem/ledgermem-openai-assistants.git"
  },
  "dependencies": {
    "@ledgermem/memory": "^0.1.0"
  },
  "peerDependencies": {
    "openai": ">=4.50.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18.17.0"
  }
}
